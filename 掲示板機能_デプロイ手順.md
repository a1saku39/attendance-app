# 🚨 掲示板機能追加後のデプロイ手順

## 問題の原因

「必要なパラメータが不足している」というエラーが出る場合、**Google Apps Scriptのデプロイが古いバージョンのまま**の可能性が高いです。

## 解決方法：デプロイの更新

### 手順1: Google Apps Script エディタを開く

1. スプレッドシートを開く
2. メニューから「拡張機能」→「Apps Script」を選択

### 手順2: コードが最新か確認

1. `Code.gs` を開く
2. 以下のコードが含まれているか確認：

```javascript
} else if (action === 'getBoardPosts') { // 掲示板投稿取得
   debugSheet.appendRow([new Date(), 'API実行: getBoardPosts']);
   return getBoardPosts();
} else if (action === 'postBoardMessage') { // 掲示板投稿作成
   debugSheet.appendRow([new Date(), 'API実行: postBoardMessage']);
   return postBoardMessage(e);
}
```

3. 含まれていない場合は、最新の `Code.gs` を貼り付けて保存（Ctrl+S）

### 手順3: デプロイを更新（重要！）

#### 方法A: 既存デプロイの更新（推奨）

1. 右上の「デプロイ」→「デプロイを管理」をクリック
2. 現在のデプロイの右側にある ✏️（編集）アイコンをクリック
3. 「バージョン」→「新バージョン」を選択
4. 説明に `掲示板機能追加` と入力
5. **「デプロイ」をクリック**
6. 完了メッセージが表示されたら「完了」をクリック

#### 方法B: 新規デプロイ

1. 右上の「デプロイ」→「新しいデプロイ」をクリック
2. 歯車アイコン⚙️をクリック→「ウェブアプリ」を選択
3. 以下の設定：
   - **説明**: `掲示板機能追加`
   - **次のユーザーとして実行**: 自分
   - **アクセスできるユーザー**: 全員
4. 「デプロイ」をクリック
5. 新しいURLが表示されたら、それをコピーして各HTMLファイルのGAS URL設定に貼り付け

### 手順4: 動作確認

1. 掲示板ページ（Board.html）を開く
2. 「管理者用：お知らせを投稿する」をクリック
3. フォームに入力：
   - 種類: バージョンアップ
   - タイトル: テスト投稿
   - 内容: これはテストです
   - 投稿者: テスト
4. 「投稿する」をクリック
5. 「投稿しました！」と表示されれば成功

## トラブルシューティング

### それでもエラーが出る場合

#### 1. ブラウザのキャッシュをクリア

```
Ctrl + Shift + Delete
→ キャッシュをクリア
→ ページを再読み込み（Ctrl + F5）
```

#### 2. GAS URLが正しいか確認

1. メイン画面（index.html）を開く
2. 画面下部の「設定（管理者用）」を開く
3. GAS WebアプリURLが正しく設定されているか確認
4. 必要に応じて最新のURLに更新して「保存」

#### 3. スプレッドシートの権限を確認

1. スプレッドシートを開く
2. 右上の「共有」をクリック
3. 「リンクを知っている全員」に変更（または必要な権限を付与）

#### 4. Apps Scriptの実行ログを確認

1. Apps Scriptエディタを開く
2. 左メニューの「実行数」をクリック
3. 最新の実行ログを確認
4. エラーメッセージがあれば、その内容を確認

#### 5. console.logを確認

1. Apps Scriptエディタを開く
2. 左メニューの「実行数」をクリック
3. 最新の実行をクリック
4. ログタブで詳細なデバッグ情報を確認：
   - `postBoardMessage called`
   - `e.postData.contents: ...`
   - `Parsed jsonData: ...`

## よくある間違い

### ❌ デプロイを更新していない
→ コードを変更しただけでは反映されません。必ずデプロイを更新してください。

### ❌ 古いURLを使っている
→ 新規デプロイした場合は、新しいURLをHTMLファイルの設定に保存してください。

### ❌ ブラウザキャッシュが残っている
→ Ctrl+F5で強制再読み込みしてください。

## 成功の確認方法

1. 掲示板ページで投稿が成功する
2. スプレッドシートに「掲示板」シートが自動作成される
3. 「掲示板」シートに投稿データが追加される
4. 投稿一覧に新しい投稿が表示される

## サポート

上記の手順を試してもエラーが解決しない場合は、以下の情報を添えてお問い合わせください：

- エラーメッセージの全文
- ブラウザの開発者ツール（F12）のConsoleタブのエラー
- Apps Scriptの実行ログのスクリーンショット
- 使用しているブラウザとバージョン
